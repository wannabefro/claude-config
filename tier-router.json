{
  "mode": "all-except-skip",
  "default": "sonnet",
  "_route_note": "Keys must be the agent name AS DISPATCHED, which for plugin agents is namespaced (plugin:agent). Bare names silently never match — all three entries here were dead until 2026-07-27. Plan is deliberately absent: it lives in skip, and skip outranks route, so listing it here only looked like it did something.",
  "route": {
    "pr-review-toolkit:silent-failure-hunter": "opus",
    "pr-review-toolkit:type-design-analyzer": "opus"
  },
  "haiku": [
    "statusline-setup",
    "impeccable:impeccable-manual-edit-applier"
  ],
  "_skip_note": "fable-planner and fable-debugger were retired on 2026-07-27 — Opus 5 covers what they were for, and fable-planner drew a correction on 11 of 12 dispatches. Fable itself is NOT gone: it is still the council's escalation seat, addressed there by explicit model, which never touches this route table.",
  "skip": ["fork", "Plan", "deep-reasoner"],
  "deny": [
    {
      "agent": "implementer",
      "model": "opus",
      "reason": "Never promote the implementer to Opus or above (rules/orchestration.md): plan harder so Sonnet can write it, keep it in the main thread, or route to a different family via /codex:rescue."
    },
    {
      "agent": "implementer",
      "model": "fable",
      "reason": "Never promote the implementer to Opus or above (rules/orchestration.md): plan harder so Sonnet can write it, keep it in the main thread, or route to a different family via /codex:rescue."
    }
  ],
  "debug": false
}
