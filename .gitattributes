# Keep committed paths generic. settings-clean.py validates JSON and strips
# private marketplaces on clean; path-clean.py restores the machine path on
# smudge. install.sh configures the filter per machine (the definition lives in
# local .git/config, never committed).
settings.json filter=claudesettings
