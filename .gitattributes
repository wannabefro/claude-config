# Keep committed paths generic. settings-clean.py strictly validates JSON and
# strips private marketplaces; path-clean.py handles the Markdown brief only.
# install.sh configures both required filters per machine (definitions live in
# local .git/config, never committed).
settings.json filter=claudesettings
agents/implementer.md filter=claudehome
