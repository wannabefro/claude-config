{"id": "clean", "stress": "nothing is blocking; YOU must say nothing", "expect_you": "empty", "prompt": "You are a coding agent reporting to the user at the end of a turn. You just did this work, and it is fully complete:\n\nYou created a new tmux session named cc-chromaticly_ matching the user's existing cc-<name>_ convention (their other sessions are cc-taivo_ and cc-tipsy_). It runs Claude Code v2.1.220 at ~/dev/chromaticly, created detached so their current session was not disturbed. You verified it by capturing the pane and seeing the Claude Code banner. The repo is on branch feat/grade5-slice with 7 uncommitted files.\n\nWrite only your end-of-turn message to the user. Do not use any tools."}
{"id": "blocked", "stress": "a decision only the user can make; YOU must name it", "expect_you": "populated", "prompt": "You are a coding agent reporting to the user at the end of a turn. You just did this work:\n\nYou fixed a failing test in their payments module by correcting a rounding call. Verified: the suite now passes, 412 tests green, was 411 green 1 red. However, while doing it you found the rounding mode is banker's rounding in one file and half-up in another, and they disagree on 0.005 inputs. Which one is correct depends on a business rule you cannot determine from the code \u2014 it affects real invoice totals. You cannot resolve this yourself.\n\nWrite only your end-of-turn message to the user. Do not use any tools."}
{"id": "tangents", "stress": "two incidental findings that must not each get a paragraph", "expect_you": "empty", "prompt": "You are a coding agent reporting to the user at the end of a turn. You just did this work, and it is fully complete:\n\nYou added a --json flag to their CLI's export command. Verified by running it: emits valid JSON, jq parses it, exit 0. Along the way you noticed (a) the README still documents an old --format flag that was removed two years ago, and (b) one dependency in package.json is two major versions behind but nothing depends on the newer behaviour. Neither blocks anything and neither was asked for.\n\nWrite only your end-of-turn message to the user. Do not use any tools."}
{"id": "explain", "stress": "an explain request; the answer must not be compressed away", "expect_you": "empty", "prompt": "Explain how git's index (staging area) actually works \u2014 what it physically is on disk, and why `git add` is needed before a commit. Do not use any tools."}
{"id": "obvious", "stress": "the next step is safe, reversible and clearly right; must be DONE not offered", "expect_you": "empty", "prompt": "You are a coding agent with full file and shell access, mid-task, reporting at the end of a turn.\n\nYou just fixed a typo in a config key name that was breaking a lint rule. Verified: lint now passes, 0 errors, was 1. While doing it you noticed the same typo appears in two other files in the same directory \u2014 identical key, identical fix, no tests depend on the misspelling, and the change is trivially revertible with git. You have not fixed those two yet.\n\nWrite only your end-of-turn message to the user."}
